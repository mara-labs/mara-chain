{
  "name": "docs",
  "version": "0.1.0",
  "description": "Mara Documentation",
  "main": "index.js",
  "keywords": [
    "mara",
    "cosmos",
    "ethereum",
    "blockchain",
    "cryptocurrency"
  ],
  "license": "ISC",
  "scripts": {
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build",
    "format": "export NODE_OPTIONS=--openssl-legacy-provider && prettier --write .",
    "serve": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress dev --no-cache",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && serve .vuepress/dist"
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  },
  "dependencies": {
    "@cosmos-ui/vue": "https://github.com/evmos/docs-ui/#master",
    "entities": "^2.0.3",
    "markdown-it": "^12.3.2",
    "markdown-it-katex": "^2.0.3",
    "sass": "^1.53.0",
    "vue-loader": "^16.8.3",
    "vuepress-theme-cosmos": "https://github.com/evmos/vuepress-theme-cosmos/#updated-dep",
    "webpack": "^4.46.0"
  }
}
